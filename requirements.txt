# Build dependencies (install first)
setuptools>=65.0.0
wheel>=0.40.0
pip>=23.0.0

# Core Framework
flask==2.3.2
flask-cors==4.0.0
gunicorn==21.2.0

# Data & Analysis (Free - No API keys needed)
yfinance>=0.2.66
pandas>=1.5.3
numpy>=1.24.3
ta==0.11.0

# Utilities
requests==2.31.0
python-dotenv==1.0.0
plotly==5.17.0

# AI Analysis — new google-genai SDK (replaces deprecated google-generativeai)
google-genai>=1.0.0

# Schema validation for AI responses
pydantic>=2.0.0

# Retry logic for external API calls
tenacity>=8.2.0

# Multi-Agent System & MCP (Model Context Protocol)
# MCP enables agent communication and coordination
# No additional packages needed - using custom MCP implementation

# Zerodha Integration (Optional - Only if you need portfolio features)
kiteconnect==4.2.0